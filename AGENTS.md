# AGENTS.md

非同期処理によるミューテックス (相互排他) を提供する TypeScript ライブラリ。

## コマンド

- テスト: `npx vitest run`（CI では `mise run test`）
- 型チェック: `npx tsc --noEmit`
- ビルド: `npm run build`
- リント: `npx oxlint`
- フォーマット: `npx oxfmt`

## 規約

- ソースは `src/`、テストは `tests/` に配置する。
